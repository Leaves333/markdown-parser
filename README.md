# markdown-parser

making my own markdown to html parser for a (future) static site generator???
made with cmake and c++20, and testing with Catch2, because i have such a poor
understanding of how to actually build a c++ project

---

to build the project:

initialize build directory:
```sh
cmake -B build
```

build and run (but there's nothing in the main file yet :skull:):
```sh
cmake --build build && ./build/parser
```
